// Main function where the program execution begins
fn main() {
    // Create a vector `v` with elements 1, 2, and 3
    let v = vec![1, 2, 3];

    // Print the vector `v` using the debug format
    println!("{:?}", v);
}
